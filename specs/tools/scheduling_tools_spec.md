## 1. Purpose

Defines tools for timezone resolution, slot generation, and booking.

## 2. Tool Set

* `resolve_timezone`
* `get_calendar_slots`
* `propose_slots`
* `book_discovery_call`

## 3. Rules

* explicit prospect confirmation required before booking
* timezone assumptions must be logged
* booking failures must preserve state consistency

---
